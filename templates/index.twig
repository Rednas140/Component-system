{% extends "_layout" %}

{% block content %}
    {# Loop for getting the components #}
    {% if entry is defined %}
        {% for block in entry.components.all() %}
            {% set componentType = block.type %}
                {% include 'components/'~componentType~'.twig' ignore missing %}
        {% else %}
        {% endfor %}
    {% endif %}
{% endblock %}