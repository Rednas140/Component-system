{% extends "_layout" %}

{% block content %}
    {# Loop for getting the components #}
    {% if entry is defined %}
        {% for block in entry.components.all() %}
            {% set componentType = block.type %}

            {% include 'components/'~componentType~'.twig' %}
        {% else %}
        {% endfor %}
    {% endif %}
{% endblock %}